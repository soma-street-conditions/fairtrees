{
  "name": "fairtrees",
  "version": "2.0.0",
  "private": true,
  "description": "Tracker for empty street-tree basins reported to SF 311",
  "type": "module",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
